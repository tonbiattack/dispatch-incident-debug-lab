# 設計方針

本教材は、配送追跡、集荷枠、返品受付という状態変化の多い業務を題材にします。Frontend、BFF、Backend、データストア、外部配送連携、実行環境を分けることで、同じ画面症状でも原因が一箇所に限定されない構造にします。

特に、追跡の一時的な未登録結果をキャッシュしてしまう問題、並行予約で残枠を失う問題、返品時に一部だけコミットされる問題、配送画面のキャッシュが別の追跡番号と衝突する問題を通じて、状態・時間・境界をまたぐ調査を練習します。

Node側はTypeScriptコンパイラとNode標準テストランナー、BackendはJUnitを使います。テストは行番号や実装詳細ではなく、下流失敗の透過、契約上の必須値、キャッシュキー、競合検知、Transaction境界といった観測可能な契約を固定します。
