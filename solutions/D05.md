# D05 解答: XML namespace契約

## 現象と原因
HTTP成功でもXMLのnamespaceや要素名が契約と異なれば変換できません。

## 調査順序
生のXML、Content-Type、namespace URI、要素名、パーサー例外を順に確認します。

## 修正内容と回帰テスト
namespaceを考慮して必須要素を検証します。

追加する回帰テストは、正しいXMLは読め、誤ったnamespaceは拒否することです。

## 再発防止
外部契約のサンプル本文をバージョン管理します。
