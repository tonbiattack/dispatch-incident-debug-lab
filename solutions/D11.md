# D11 解答: Kubernetes設定ドリフト

## 現象と原因
ComposeとKubernetesで環境変数名、Service DNS、probeパスがずれると接続またはReady判定に失敗します。

## 調査順序
Podイベント、注入済み環境変数、Service名、アプリログ、probe応答を順に確認します。

## 修正内容と回帰テスト
設定キーと実在するhealth endpointを統一します。

追加する回帰テストは、manifestが実行時設定の契約を満たすことです。

## 再発防止
ステージング起動確認とmanifest検証をCIに入れます。
