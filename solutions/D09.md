# D09 解答: OpenAPI必須値違反

## 現象と原因
生成型が必須とみなす最新イベントを実装が空で返すため、実行時に壊れます。

## 調査順序
Frontend契約型、BFF応答、Backend応答、OpenAPI定義を同じフィールドで比較します。

## 修正内容と回帰テスト
Backendで必須値を保証し、境界で検証します。

追加する回帰テストは、必須の配送表示値が空でないことです。

## 再発防止
契約変更はOpenAPIと実装を同時に更新します。
