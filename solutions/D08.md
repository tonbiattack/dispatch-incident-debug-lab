# D08 解答: 返品受付のTransaction境界

## 現象と原因
返品と荷物状態更新が同一トランザクションでないため、途中例外でも一部更新が確定します。

## 調査順序
例外ログ、両テーブルの状態、呼出し階層、Transaction属性の順に確認します。

## 修正内容と回帰テスト
業務単位のサービスメソッドをTransactionalにします。

追加する回帰テストは、失敗時に両方の更新が残らないことです。

## 再発防止
非同期化時は補償処理を設計します。
