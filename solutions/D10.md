# D10 解答: requestId伝播

## 現象と原因
BFFでIDを作っても外部HTTPヘッダーへ渡さなければ分散ログを結べません。

## 調査順序
Browser、BFF、Backend、外部連携の順に同じヘッダーを確認します。

## 修正内容と回帰テスト
全HTTPクライアントでX-Request-Idを設定し、応答にも返します。

追加する回帰テストは、外部呼出に同じ相関IDが渡ることです。

## 再発防止
共通インターセプターを利用します。
