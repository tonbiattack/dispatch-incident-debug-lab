# D02 解答: 空追跡結果のキャッシュ汚染

## 現象と原因
一時的な未登録をRedisへ保存すると、配送事業者が復旧してもTTL中は古い値を返します。

## 調査順序
WireMockの現在応答、Redisの値とTTL、Backendのキャッシュヒットを順に比較します。外部が正常でRedisが空なら保存条件へ絞れます。

## 修正内容と回帰テスト
空の追跡結果を通常TTLで保存しないようにします。

追加する回帰テストは、空結果がキャッシュ保存対象でないことです。

## 再発防止
障害種別と空結果を区別し、鮮度要件を明示します。
